# TODO: 获得管理员权限后完成钉钉工作台接入

## 状态

延期。当前开发者没有创建、配置和发布企业内部应用所需的钉钉管理员权限。

## 恢复条件

- 管理员授予应用开发/管理权限，或由管理员协助完成开发者后台配置。
- 准备可从钉钉客户端访问的 HTTPS 测试域名。
- 确认测试版本的人员可见范围与后端稳定用户 ID 白名单。

## 后续动作

1. 创建或选择“智询”企业内部应用并配置 PC/移动端网页首页。
2. 申请端内免登所需的最小接口权限并配置服务器出口 IP。
3. 发布工作台网页应用，仅开放批准的测试人员范围。
4. 在 PC、Android、iOS 验证免登、失败关闭和直接 URL 访问控制。

## 约束

- 工作台可见范围不能替代服务端授权。
- 服务端只按钉钉稳定 `userId/unionId` 授权，不按姓名授权。
- 凭据和真实人员标识只进入部署环境，不进入仓库或普通日志。
